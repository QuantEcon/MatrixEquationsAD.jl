var documenterSearchIndex = {"docs":
[{"category":"section","location":"klein_map/#Klein-Policy-Map","page":"Klein Policy Map","text":"klein_map(A, B; threshold) returns the Klein/Sims first-order policy (g_x h_x) for a DSGE pencil (A B) with the Blanchard-Kahn selection at margin tau = textttthreshold. Two API forms exist:\n\nklein_map(A, B; threshold) — heap-allocated, returns a NamedTuple (; g_x, h_x).\nklein_map!(g_x, h_x, A, B; threshold) — mutating, writes into caller-supplied buffers; the in-place AD rules use a more memory-efficient reduced-Sylvester factorization.\n\nImplementation pointers:\n\nsrc/klein_map.jl — primal solve via ordered generalized Schur (BK ordering on |α| ≥ (1 − τ)|β|) followed by the Klein/Sims algebra.\next/klein_map_derivatives.jl — _klein_bigk_plan / _klein_bigk_jvp / _klein_bigk_vjp for the OOP path; _klein_structured_plan / _klein_structured_jvp / _klein_structured_vjp for the in-place path.\next/enzyme_klein_map.jl and ext/forwarddiff_klein_map.jl — the AD frontends.","title":"Klein Policy Map"},{"category":"section","location":"klein_map/#Where-the-Primal-Comes-From","page":"Klein Policy Map","text":"Consider the linearised equilibrium of a DSGE model around a deterministic steady state. Partition the state into n_x predetermined variables x_t and n_y non-predetermined (jump) variables y_t, and write z_t = x_t y_t in mathbbR^n with n = n_x + n_y. The linearised model can be cast in the pencil form\n\nAmathbbE_tz_t+1 + Bz_t = 0\nqquad\nA B in mathbbR^n times n\n\nKlein (2000) shows that the unique bounded solution under the Blanchard-Kahn condition takes the linear form\n\ny_t = g_x x_t\nqquad\nx_t+1 = h_x x_t + text(shock terms)\n\ni.e. the jumps are an affine function of the predetermined state, and the predetermined state follows an autonomous linear law of motion. The package's klein_map(A, B; threshold) returns exactly that (g_x h_x).","title":"Where the Primal Comes From"},{"category":"section","location":"klein_map/#Algorithm","page":"Klein Policy Map","text":"Stacked column-wise as z = xy, define the embedding\n\nPsi\n=\nbeginbmatrix\nI_n_x \ng_x\nendbmatrix\nin mathbbR^n times n_x\n\nso that z_t = Psix_t under the policy, and z_t+1 = Psih_xx_t. Substituting into the pencil equation,\n\nAPsih_xx_t + BPsix_t = 0\nqquad textfor all  x_t\n\nwhich is the implicit policy equation\n\nF(A B g_x h_x)\n=\nAPsi h_x + BPsi\n=\n0\nquadinmathbbR^n times n_x\ntagF\n\nklein_map constructs the solution by Klein's QZ-based procedure:\n\nTake the real generalised Schur decomposition A = Q S Z^top, B = Q T Z^top with Q, Z orthogonal, S quasi-upper-triangular, T upper triangular. Eigenvalues are the pairs (alpha_ibeta_i).\nReorder so that the unstable generalised eigenvalues (alpha_i ge (1 - tau)beta_i) come first. tau is the threshold keyword; the BK count n_x is the number of selected eigenvalues and must equal the predetermined-block dimension.\nPartition into b = 1n_x (unstable, \"big\") and l = (n_x+1)n (stable) blocks, and read off\ng_x = -Z_l l^-top Z_b l^top\nqquad\nh_x = -mathrmblob^-1 S_b b^-1 T_b bmathrmblob\nquad\nmathrmblob = Z_b b^top + Z_l b^topg_x\n\nThe construction is a direct port of DifferentiablePerturbation.jl's first_order_perturbation!; see src/klein_map.jl for the indexing.","title":"Algorithm"},{"category":"section","location":"klein_map/#Post-Conditions","page":"Klein Policy Map","text":"At the policy returned by klein_map, the following hold (each is unit-tested on the RBC, RBC_SV, SGU, FVGQ, and SW07PFEIFER fixtures):\n\nImplicit policy residual: F(A B g_x h_x) = APsih_x + BPsi = 0 in floating-point arithmetic (typical residual is below 10^-8 on the standard fixtures and is what test/test_klein_map.jl checks).\nSaddle-path: every x_0 in mathbbR^n_x generates a bounded trajectory z_t = Psi h_x^t x_0.\nStable transition: the spectrum of h_x lies strictly inside the unit disc (rho(h_x)  1), with eigenvalues equal to the stable generalised eigenvalues beta_ialpha_i of the inverse pencil.\nStatic algebraic post-conditions (consequences of the QZ structure the algorithm reads off): Z_l l^top g_x = -Z_b l^top and mathrmblob h_x = -S_b b^-1 T_b bmathrmblob.\n\nThe factorisation assumptions for the AD rules below are:\n\nthe BK split returns exactly n_x unstable eigenvalues with 0  n_x  n;\nZ_l l and mathrmblob are nonsingular (used in the primal);\nthe linearisation of F (the Jacobian K below) is nonsingular at the primal solution.\n\nFailures throw ErrorException (BK / consistency check) or the underlying LAPACK exception (Schur / LU).\n\nCalling klein_map on the canonical RBC pencil (assembled by RBCExampleMatrices.rbc_first_order_assembly(p) from test/example_matrices/rbc.jl, itself a readable translation of DifferentiablePerturbation.jl's code-generated RBC.first_order_assembly!):\n\njulia> using MatrixEquationsAD\n\njulia> include(joinpath(pkgdir(MatrixEquationsAD), \"test\", \"example_matrices\", \"rbc.jl\"));\n\njulia> using .RBCExampleMatrices: rbc_first_order_assembly\n\njulia> A, B, n_x = rbc_first_order_assembly([0.5, 0.95, 0.2, 0.02, 0.01, 0.01]);\n\njulia> r = klein_map(A, B; threshold = 1.0e-6);\n\njulia> size(r.g_x), size(r.h_x)\n((3, 2), (2, 2))\n\nr.g_x[1, 2] is the static response of consumption to a TFP innovation; r.h_x[1, 2] is how a TFP shock propagates into next period's capital.","title":"Post-Conditions"},{"category":"section","location":"klein_map/#Why-AD-Rules-Operate-on-(F),-Not-on-the-QZ-Factors","page":"Klein Policy Map","text":"In principle one could compute (bar A bar B) by differentiating the generalised-Schur factors (S T Q Z) and then back-propagating through the Klein algebra. We do not do that, and the docs derive the AD rules directly from the implicit residual (F). The reasons:\n\nThe QZ factors are not unique. Within each repeated or clustered eigenvalue group, Q and Z admit a gauge freedom: any orthogonal transformation that commutes with the Schur block structure leaves (S T) unchanged. The tangent of (Q Z) is therefore ill-defined precisely where eigenvalues coalesce — e.g. the FVGQ pencil has multiple clusters at alphabeta = 1.\nBlock structure changes are discontinuous. Real-Schur S mixes 1times 1 real blocks with 2times 2 complex-conjugate blocks. A perturbation that pushes a real pair into a complex pair (or vice versa) changes the block layout discontinuously. The reordering step that enforces the BK selection has the same issue at threshold crossings.\nKlein's policy (g_x h_x) is gauge-invariant. It depends only on the stable deflating subspace, not on the particular orthogonal basis chosen for it. Differentiating (F) reflects that invariance: the tangents (dg_x dh_x) are uniquely defined even when (dQ dZ) are not.\nThe QZ-tangent rule needs perturbation theory the IFT does not. The implicit-function theorem applied to (F) only requires nonsingularity of the linearised operator K below, which holds generically; the QZ-tangent approach needs eigenvalue/eigenvector perturbation bounds that degrade as clusters tighten.\n\nSo the AD rules differentiate (F) itself. The locally constant ingredients (BK selection, real-vs-complex block layout, threshold position) are treated as fixed; the assumption is that no eigenvalue crosses the threshold under perturbation.","title":"Why AD Rules Operate on (F), Not on the QZ Factors"},{"category":"section","location":"klein_map/#Jacobian-of-the-Implicit-Equation","page":"Klein Policy Map","text":"Let E_y = 0_n_x times n_y I_n_y in mathbbR^n times n_y, and split the columns of A and B as A_2 = A E_y, B_2 = B E_y (the trailing n_y columns). Differentiating F = 0 at fixed (A B) gives\n\nAPsidh_x\n+\nbigl(A_2  dg_x  h_x + B_2  dg_xbigr)\n=\n-dAD - dBPsi\n\nVectorising column-major and stacking the unknowns as operatornamevec(dh_x)operatornamevec(dg_x) in mathbbR^n n_x,\n\nK\nbeginbmatrix\noperatornamevec(dh_x) \noperatornamevec(dg_x)\nendbmatrix\n=\n-operatornamevecbigl(dAD + dBPsibigr)\n\nwith\n\nK\n=\nBigl\nI_n_xotimes APsi\nBig\nh_x^topotimes A_2 + I_n_xotimes B_2\nBigr\ninmathbbR^n n_x times n n_x\n\nTwo equivalent linear-algebra representations are implemented and produce identical tangents up to floating-point roundoff. Both are correct on degenerate-cluster pencils (e.g. the FVGQ fixture) because they differentiate the implicit equation rather than the non-unique QZ factors.","title":"Jacobian of the Implicit Equation"},{"category":"section","location":"klein_map/#Big-K-Factorisation-(OOP-klein_map)","page":"Klein Policy Map","text":"The OOP rule builds K once as a dense (n n_x) times (n n_x) block matrix and stores its LU factorisation in the plan returned by _klein_bigk_plan. Both forward and reverse modes reuse this single factorisation.","title":"Big-K Factorisation (OOP klein_map)"},{"category":"section","location":"klein_map/#ForwardDiff-/-Enzyme-JVP","page":"Klein Policy Map","text":"For each tangent direction (dA dB),\n\nbeginbmatrix\noperatornamevec(dh_x) \noperatornamevec(dg_x)\nendbmatrix\n=\nK^-1operatornamevecbigl(-(dAD + dBPsi)bigr)\n\n_klein_bigk_jvp forms the right-hand side R = -(dAD + dBPsi), performs one LU back-substitution, and reshapes the solution into (dh_x dg_x). ForwardDiff chunks of width N and Enzyme BatchDuplicated of width N both issue N such back-substitutions against the shared factorisation.","title":"ForwardDiff / Enzyme JVP"},{"category":"section","location":"klein_map/#Enzyme-VJP","page":"Klein Policy Map","text":"Pack the output cotangents in the same block order as K:\n\nu\n=\nbeginbmatrix\noperatornamevec(bar h_x) \noperatornamevec(bar g_x)\nendbmatrix\ninmathbbR^n n_x\nqquad\nlambda = K^-top u\nqquad\nLambda = operatornamereshape(lambda n n_x)\n\nBecause the residual R depends on A and B only through R = -(dAD + dBPsi), the parameter cotangents are\n\nbar A\nmathrel+=\n-LambdaD^top\n=\n-Lambdah_x^topPsi^top\nqquad\nbar B\nmathrel+=\n-LambdaPsi^top\n\n_klein_bigk_vjp performs the single transposed LU solve, reshapes lambda, and applies these outer products.","title":"Enzyme VJP"},{"category":"section","location":"klein_map/#Reduced-Sylvester-Factorisation-(in-place-klein_map!)","page":"Klein Policy Map","text":"The in-place rule never materialises the full K. It instead builds the n times n block\n\nC_0\n=\nbeginbmatrix\nAPsi  B_2\nendbmatrix\ninmathbbR^n times n\n\nLU-factorises C_0, solves once for the auxiliary matrix J = C_0^-1 A E_y in mathbbR^n times n_y, and splits it into\n\nJ\n=\nbeginbmatrix\nJ_x \nJ_y\nendbmatrix\nqquad\nJ_x in mathbbR^n_x times n_y\nqquad\nJ_y in mathbbR^n_y times n_y\n\nReal Schur factorisations J_y = Q_y S_y Q_y^top and h_x = Q_h S_h Q_h^top are computed once and reused.","title":"Reduced-Sylvester Factorisation (in-place klein_map!)"},{"category":"section","location":"klein_map/#JVP","page":"Klein Policy Map","text":"Let Y = C_0^-1 R with R = -(dAD + dBPsi), and split Y = Y_xY_y. Premultiplying the tangent equation by C_0^-1 and matching block rows gives\n\nboxed\ndg_x + J_ydg_xh_x = Y_y\n\n\nwhich is a discrete Sylvester / Stein equation. In the Schur frame tilde X = Q_y^top dg_x Q_h it becomes\n\nS_ytilde XS_h + tilde X = Q_y^top Y_y Q_h\n\nsolved in place by MatrixEquations.sylvds!. Back-transforming and substituting into the h_x block,\n\ndh_x = Y_x - J_xdg_xh_x","title":"JVP"},{"category":"section","location":"klein_map/#VJP","page":"Klein Policy Map","text":"Given output cotangents (bar g_x bar h_x), the elimination above flows backwards as follows. The dh_x formula contributes to bar Y_x and induces a correction on the dg_x cotangent:\n\nbar Y_x = bar h_x\nqquad\ntildebar g_x\n=\nbar g_x - J_x^topbar h_xh_x^top\n\nThe corrected tildebar g_x is the right-hand side of the adjoint Stein equation\n\nJ_y^topZh_x^top + Z = tildebar g_x\nqquad\nbar Y_y = Z\n\nagain solved with sylvds!(S_y, S_h, ⋅; adjA = true, adjB = true) in Schur coordinates. Reassembling\n\nbar Y\n=\nbeginbmatrix\nbar h_x \nZ\nendbmatrix\nqquad\nLambda = C_0^-topbar Y\n\nrequires one transposed LU back-substitution against the same C_0 factorisation. The parameter cotangents are then identical to the big-K form:\n\nbar A\nmathrel+=\n-Lambdah_x^topPsi^top\nqquad\nbar B\nmathrel+=\n-LambdaPsi^top","title":"VJP"},{"category":"section","location":"klein_map/#Which-Path-Wins","page":"Klein Policy Map","text":"Pencil size Big-K (OOP) Reduced Sylvester (in-place)\nSmall (n ≲ 15) Faster — LU is cheap, single multi-RHS solve. Setup overhead (two Schurs + one LU) dominates.\nLarge (n ≳ 30) LU of (n n_x)^2 becomes expensive. Wins; only n_y times n_y Schur + per-tangent sylvds!.\n\nAt the SW07PFEIFER pencil (n = 42, n_x = 20) the in-place reverse runs roughly five times faster than the OOP reverse. At the RBC pencil (n = 5) the OOP path wins by ~50%.","title":"Which Path Wins"},{"category":"section","location":"klein_map/#SMatrix-Dispatch","page":"Klein Policy Map","text":"StaticArrays support requires explicit Val(n_x) so the return type is statically sized:\n\nusing StaticArrays\nAs = SMatrix{5, 5, Float64}(A);  Bs = SMatrix{5, 5, Float64}(B)\nr = klein_map(As, Bs, Val(2); threshold = 1.0e-6)\n@assert r.g_x isa SMatrix{3, 2, Float64}\n@assert r.h_x isa SMatrix{2, 2, Float64}\n\nWithout the Val, an SMatrix input falls through to the heap dispatch and returns plain Matrix outputs — type-stable code-gen requires the BK split to be known at compile time, and a runtime BK count cannot satisfy that.","title":"SMatrix Dispatch"},{"category":"section","location":"klein_map/#Examples","page":"Klein Policy Map","text":"All snippets below assemble the RBC pencil from a parameter vector and then call the policy map, so AD can flow from p through the assembly into klein_map:\n\ninclude(joinpath(pkgdir(MatrixEquationsAD), \"test\", \"example_matrices\", \"rbc.jl\"))\nusing .RBCExampleMatrices: rbc_first_order_assembly\n\np = [0.5, 0.95, 0.2, 0.02, 0.01, 0.01]\nA, B, n_x = rbc_first_order_assembly(p)\nn_y = size(A, 1) - n_x\n\nA gradient of a policy-summary functional with respect to the parameter vector uses ForwardDiff straight through:\n\nusing ForwardDiff\nfunction policy_loss(p)\n    A, B, _ = rbc_first_order_assembly(p)\n    r = klein_map(A, B; threshold = 1.0e-6)\n    return sum(r.g_x) + sum(r.h_x)\nend\n∇p = ForwardDiff.gradient(policy_loss, p)\n\nForwardDiff Jacobian of the flattened policy against the flattened pencil:\n\nusing ForwardDiff\nusing MatrixEquationsAD\n\nn = size(A, 1)\nfunction klein_vec(x)\n    A_x = reshape(x[1:(n*n)], n, n)\n    B_x = reshape(x[(n*n+1):end], n, n)\n    r = klein_map(A_x, B_x; threshold = 1.0e-6)\n    return [vec(r.g_x); vec(r.h_x)]\nend\nJ = ForwardDiff.jacobian(klein_vec, [vec(A); vec(B)])\n# size(J) == (n_y*n_x + n_x*n_x, 2*n*n) == (10, 50) for the RBC pencil.\n\nEnzyme batched forward of width 4 on a scalar loss:\n\nusing Enzyme, Random\n\nrng = MersenneTwister(0)\ndA_lanes = ntuple(_ -> randn(rng, n, n), Val(4))\ndB_lanes = ntuple(_ -> randn(rng, n, n), Val(4))\n\nloss(A, B) = (r = klein_map(A, B); sum(r.g_x) + sum(r.h_x))\n\n(out,) = Enzyme.autodiff(\n    Forward, loss, BatchDuplicated,\n    BatchDuplicated(copy(A), dA_lanes),\n    BatchDuplicated(copy(B), dB_lanes),\n)\n# `out` is a NamedTuple of the four scalar JVPs, one per tangent direction.\n\nIn-place reverse on a klein_map! call:\n\ng_x = zeros(n_y, n_x);  h_x = zeros(n_x, n_x)\nA_bar = zero(A);  B_bar = zero(B)\n\nEnzyme.autodiff(\n    Reverse, (g, h, A, B) -> (klein_map!(g, h, A, B); sum(g) + sum(h)),\n    Active,\n    Duplicated(g_x, zero(g_x)),\n    Duplicated(h_x, zero(h_x)),\n    Duplicated(copy(A), A_bar),\n    Duplicated(copy(B), B_bar),\n)\n\nThe klein_map! rule uses the reduced-Sylvester path; the OOP klein_map loss above routes through the big-K rule instead.","title":"Examples"},{"category":"section","location":"lyapd/#Discrete-Lyapunov-(Schur)","page":"Discrete Lyapunov (Schur)","text":"lyapd(A, C) solves the discrete Lyapunov equation\n\nA X A^top - X + C = 0\n\nusing the Schur-based Bartels–Stewart algorithm from MatrixEquations.jl: the upstream solver Schur-factorises A once and runs a triangular sweep (MatrixEquations.lyapds!) on the transformed right-hand side. For non-toy state dimensions this is the right default — the cost is O(n^3) per solve, vs. O(n^6) for the Kronecker-vec form. MatrixEquationsAD provides custom AD rules that wrap that solver in a cache-aware shadow so a single schur(A) is reused across all tangent / cotangent directions.\n\nImplementation pointers:\n\next/enzyme_lyapunov.jl — LyapDSchurCache, lyapdfactor, lyapdsolve, lyapdadjointsolve, the cache-aware lyapd(A, C) shadow, and the Enzyme forward / reverse rules.\next/forwarddiff_lyapunov.jl — the ForwardDiff Dual dispatch on lyapd(A, C), which uses the same cache primitives.\nMatrixEquations.lyapds! — upstream in-place kernel that operates on A in real or complex Schur form (adj = false / true for transpose variants).\n\nMatrixEquationsAD also exports lyapd!(X, A, C), which writes the solution into a caller-supplied X and returns nothing. The in-place form shares the same Schur cache plumbing and carries an analogous full set of AD rules — ForwardDiff Dual dispatch, Enzyme forward (Duplicated / BatchDuplicated), and Enzyme reverse (augmented_primal + reverse). The reverse rule stashes the Schur factors on the tape so the adjoint pass never re-Schurs A. A cache-taking overload lyapd!(X, cache::LyapDSchurCache, C) lets the AD rules share one schur(A) across all forward tangent directions or reverse cotangents.","title":"Discrete Lyapunov (Schur)"},{"category":"section","location":"lyapd/#Primal","page":"Discrete Lyapunov (Schur)","text":"The cache-aware shadow precomputes mathrmschur(A) = (T Z), A = ZTZ^top, and stores it in a LyapDSchurCache. With that cache available, the solve transforms the right-hand side, runs the upstream Schur-form kernel, and untransforms:\n\ntilde C = Z^topCZ\nqquad\nX = Ztilde XZ^top\n\nwhere tilde X satisfies the triangular Sylvester / Lyapunov\n\nTtilde XT^top - tilde X + tilde C = 0\n\nFor textttCSymmetric this is exactly one call to lyapds!(cache.T, rhs); for general dense C the dispatch routes to sylvds!(-cache.T, cache.T, rhs; adjB = true), which solves the same triangular system without enforcing symmetry.\n\nExistence and uniqueness require that no two eigenvalues of A multiply to one. rho(A)  1 is sufficient and is the case of interest for stationary-covariance applications below.","title":"Primal"},{"category":"section","location":"lyapd/#ForwardDiff-JVP","page":"Discrete Lyapunov (Schur)","text":"For one tangent direction (dA dC), differentiating A X A^top - X + C = 0 and applying the same operator gives\n\nAdXA^top - dX\n=\n-dC - dAXA^top - AXdA^top\n\ni.e. dX solves another discrete Lyapunov equation against the same A. The ForwardDiff dispatch builds the cache once and calls lyapdsolve(cache, rhs_i) for each partial direction in the chunk — N lyapds! triangular sweeps against the shared Schur factors.\n\nThe Enzyme BatchDuplicated forward rule is structurally identical: one schur(A) per outer call, then one triangular solve per tangent.","title":"ForwardDiff JVP"},{"category":"section","location":"lyapd/#Enzyme-VJP","page":"Discrete Lyapunov (Schur)","text":"Let bar X be the cotangent on the output. Define Y by the adjoint discrete Lyapunov solve\n\nA^topYA - Y = -bar X\n\nimplemented as lyapdadjointsolve(cache, X̄) (which routes to lyapds!(cache.T, rhs; adj = true) for Symmetric cotangents and to the transposed sylvds! variant otherwise). The parameter cotangents are then\n\nbar C mathrel+= Y\nqquad\nbar A mathrel+= YAX^top + Y^topAX\n\nThe augmented primal copies the cached Schur and the primal X onto the tape; the reverse pass performs one triangular adjoint solve plus the two outer products.\n\nWhen the input C is wrapped as Symmetric, the reverse rule projects the incoming cotangent onto the symmetric manifold (replacing bar C by tfrac12(bar C + bar C^top)) before the adjoint solve. This matches the primal symmetry contract: a Symmetric parameter is differentiated only against the symmetric perturbations that lie in its parameter space.","title":"Enzyme VJP"},{"category":"section","location":"lyapd/#Example:-RBC-stationary-covariance,-end-to-end","page":"Discrete Lyapunov (Schur)","text":"The bundled RBC fixture dp_rbc_first_order_inputs() returns a NamedTuple (; A_schur, B_schur, B_shock, g_x, h_x, n_x). Pairing its h_x with Q = B_textshockB_textshock^top and solving V = h_x V h_x^top + Q gives the stationary state covariance under the Klein/Sims policy:\n\njulia> using LinearAlgebra: Symmetric\n\njulia> using MatrixEquations: lyapd\n\njulia> using MatrixEquationsAD\n\njulia> include(joinpath(pkgdir(MatrixEquationsAD), \"test\", \"example_matrices\", \"rbc.jl\"));\n\njulia> (; h_x, B_shock) = RBCExampleMatrices.dp_rbc_first_order_inputs();\n\njulia> Q = Symmetric(B_shock * transpose(B_shock));\n\njulia> V = lyapd(h_x, Q);\n\njulia> V[2, 2] ≈ 0.01^2 / (1 - 0.2^2)               # matches AR(1) closed form\ntrue\n\njulia> round(sqrt(V[2, 2]); sigdigits = 4)          # TFP stationary std. (~1%)\n0.01021\n\njulia> round(sqrt(V[1, 1]); sigdigits = 3)          # capital stationary std.\n0.265\n\nWrapping Q as Symmetric routes the AD rules onto the lyapds!-based path; passing a plain Matrix would dispatch onto the sylvds!-based path with the same result. Either way lyapd is the right call here — its cost is O(n^3) in the state dimension, and MatrixEquationsAD adds a single schur(A) cache reused across all AD directions on top.\n\nDifferentiating any summary of V with respect to the RBC parameters p works straight through the rbc_first_order_assembly → klein_map → lyapd pipeline with DifferentiationInterface; the test suite exercises both ForwardDiff and Enzyme reverse against the same closure.","title":"Example: RBC stationary covariance, end-to-end"},{"category":"section","location":"lyapdkr/#Kronecker-Discrete-Lyapunov","page":"Kronecker Discrete Lyapunov","text":"lyapdkr(A, C) solves the discrete Lyapunov equation\n\nA X A^top - X + C = 0\n\nvia the Kronecker-vectorised form. Unlike MatrixEquations.lyapd, which uses a Schur-based Bartels-Stewart solver, lyapdkr LU-factorises the full (n^2) times (n^2) Kronecker operator once and reuses that factorisation across all tangent / cotangent directions. The output is symmetrised, so nonsymmetric perturbations of C are projected onto the symmetric solution manifold.\n\nImplementation pointers:\n\nsrc/lyapdkr.jl — lyapdkrfactor, lyapdkrsolve, lyapdkradjointsolve, and the cache struct.\next/enzyme_lyapdkr.jl and ext/forwarddiff_lyapdkr.jl — AD rules.","title":"Kronecker Discrete Lyapunov"},{"category":"section","location":"lyapdkr/#Primal","page":"Kronecker Discrete Lyapunov","text":"With column-major vec and the identity operatornamevec(A X A^top) = (A otimes A)operatornamevec(X), the discrete Lyapunov equation rewrites as\n\nbigl(I_n^2 - A otimes Abigr)operatornamevec(X)\n=\noperatornamevec(C)\n\nlyapdkrfactor builds M = I - A otimes A and stores its LU factorisation in a LyapDKrLUCache. lyapdkrsolve performs one back-substitution against M and symmetrises the result:\n\nX = Pleft(operatornamereshape(M^-1operatornamevec(C) n n)right)\nqquad\nP(M) = tfrac12(M + M^top)\n\nThe factorisation assumptions are:\n\nM is nonsingular — equivalently, no pair of eigenvalues of A has product equal to one. rho(A)  1 is sufficient.\nOptional diagnostics: tol_diag bounds X_ii and check_psd rejects negative diagonals.\n\nFailures throw ErrorException.","title":"Primal"},{"category":"section","location":"lyapdkr/#ForwardDiff-JVP","page":"Kronecker Discrete Lyapunov","text":"For one tangent direction (dA dC), differentiating Moperatornamevec(X) = operatornamevec(C) gives\n\noperatornamevec(dX_mathrmraw)\n=\nM^-1operatornamevecbigl(\ndC + dAXA^top + AXdA^top\nbigr)\nqquad\ndX = P(dX_mathrmraw)\n\nThe ForwardDiff overload runs lyapdkrfactor once on the value layer and calls lyapdkrsolve(cache, RHS_i) once per partial direction inside the chunk. A chunk of width N performs N LU back-substitutions against the shared factorisation.\n\nThe Enzyme BatchDuplicated forward rule is structurally identical: one factorise, N solves.","title":"ForwardDiff JVP"},{"category":"section","location":"lyapdkr/#Enzyme-VJP","page":"Kronecker Discrete Lyapunov","text":"Let bar X be the cotangent on the output. Symmetrise first to project onto the symmetric manifold,\n\nS = P(bar X) = tfrac12(bar X + bar X^top)\n\nthen perform one transposed solve\n\noperatornamevec(Y) = M^-topoperatornamevec(S)\nqquad\nM = I - A otimes A\n\nBecause M is bilinear in A, the parameter cotangents are\n\nbar C mathrel+= Y\nqquad\nbar A mathrel+= YAX^top + Y^top AX\n\nlyapdkradjointsolve performs the symmetric projection and the transposed LU solve. The Enzyme augmented primal copies the cached factorisation and the primal X to the tape; the reverse pass performs one transposed back-substitution and the two outer-product accumulations.","title":"Enzyme VJP"},{"category":"section","location":"lyapdkr/#Static-(SMatrix)-Dispatch","page":"Kronecker Discrete Lyapunov","text":"The StaticArrays extension provides\n\nlyapdkr(A::SMatrix{n,n,T}, C::SMatrix{n,n,T}; tol_diag=Inf, check_psd=false)\n\nwhich converts to heap, calls the regular lyapdkr, and wraps the result in an SMatrix{n,n,T}. The Kronecker matrix is n^2 times n^2, so even at modest n it is past the size where an inline static LU beats LAPACK; the dispatch exists for API consistency, not speed.","title":"Static (SMatrix) Dispatch"},{"category":"section","location":"lyapdkr/#Example:-RBC-Stationary-Distribution","page":"Kronecker Discrete Lyapunov","text":"After solving for the Klein policy, the predetermined-state law of motion is\n\nx_t+1 = h_x x_t + B_textshockvarepsilon_t+1\n\nwith x = [k, z] for the RBC model. The TFP innovation feeds into the AR(1) for z and nothing else, so B_textshock = 0 sigma. The one-step innovation covariance is Q = B_textshockB_textshock^top and the stationary covariance V = h_x V h_x^top + Q solved by lyapdkr is what you'd condition on to write down the long-run distribution of (k z) or compute a Kalman filter.\n\nThe closed-form AR(1) variance sigma^2(1-rho^2) gives the analytic TFP marginal — useful as a sanity reading, and shown below using the bundled fixture:\n\njulia> using MatrixEquationsAD\n\njulia> include(joinpath(pkgdir(MatrixEquationsAD), \"test\", \"example_matrices\", \"rbc.jl\"));\n\njulia> (; h_x, B_shock) = RBCExampleMatrices.dp_rbc_first_order_inputs();\n\njulia> Q = B_shock * transpose(B_shock);\n\njulia> V = lyapdkr(h_x, Q);\n\njulia> round(sqrt(V[2, 2]); sigdigits = 4)          # stationary std. of z (% units)\n0.01021\n\njulia> V[2, 2] ≈ 0.01^2 / (1 - 0.2^2)                # matches AR(1) closed form\ntrue\n\njulia> round(sqrt(V[1, 1]); sigdigits = 3)          # std. of capital deviation\n0.265\n\nTFP fluctuates at roughly 1% in stationary equilibrium; capital, which absorbs cumulative TFP shocks through the policy term h_x12, fluctuates ~25× more.\n\nDifferentiating either V or any summary of it with respect to the RBC parameters p works straight through the rbc_first_order_assembly → klein_map → lyapdkr pipeline.\n\nEnzyme reverse mode against a scalar loss:\n\nusing LinearAlgebra: dot\nusing Enzyme\nusing MatrixEquationsAD\n\nA = [0.55  0.08; -0.04  0.42]\nC = [1.0   0.2;   0.2   0.7]\nW = [0.3  -0.1;   0.2   0.5]\n\nloss(A, C, W) = dot(W, lyapdkr(A, C))\n\nA_bar = zero(A); C_bar = zero(C)\nEnzyme.autodiff(\n    Reverse, loss, Active,\n    Duplicated(copy(A), A_bar),\n    Duplicated(copy(C), C_bar),\n    Const(W),\n)\n# A_bar = Y A X' + Y' A X and C_bar = Y, where Y solves the transposed\n# Kronecker system M' vec(Y) = vec(P(W)).","title":"Example: RBC Stationary Distribution"},{"category":"section","location":"#MatrixEquationsAD.jl","page":"Home","text":"Automatic differentiation rules for selected MatrixEquations.jl solvers and a Klein/Sims policy-function map for first-order DSGE pencils. The package supplies custom ForwardDiff Dual dispatches and Enzyme forward/reverse rules so the solvers integrate cleanly into AD-driven likelihood, calibration, and gradient-based estimation workflows.\n\nusing MatrixEquations    # primal solvers\nusing ForwardDiff\nusing Enzyme\nusing MatrixEquationsAD  # AD rules loaded on extension load","title":"MatrixEquationsAD.jl"},{"category":"section","location":"#Guide","page":"Home","text":"Klein Policy Map — implicit policy equation, JVP, VJP.\nKronecker Discrete Lyapunov — Kronecker-vec discrete Lyapunov solve and symmetric JVP/VJP.\n\nThe remaining AD rules — for lyapd, gsylv, gsylvkr, ared — follow the standard implicit-function-theorem pattern documented in DERIVATIONS.md.","title":"Guide"},{"category":"section","location":"#Quick-start:-parameters-pencil-policy","page":"Home","text":"The bundled example is a standard RBC model. Its first-order pencil (A B) is assembled from the parameter vector\n\np = alpha beta rho delta sigma Omega_1\n  = 05 095 02 002 001 001\n\n(capital share, discount factor, TFP persistence, depreciation, TFP innovation s.d., observation-noise s.d.) by RBCExampleMatrices.rbc_first_order_assembly, defined in test/example_matrices/rbc.jl. That function is a readable port of DifferentiablePerturbation.jl's code-generated RBC.first_order_assembly! with each matrix entry written in natural model variables; ForwardDiff Dual and Enzyme Duplicated perturbations of p flow through it into klein_map.\n\njulia> using LinearAlgebra: eigvals\n\njulia> using MatrixEquationsAD\n\njulia> include(joinpath(pkgdir(MatrixEquationsAD), \"test\", \"example_matrices\", \"rbc.jl\"));\n\njulia> using .RBCExampleMatrices: rbc_first_order_assembly\n\njulia> p = [0.5, 0.95, 0.2, 0.02, 0.01, 0.01];\n\njulia> A, B, n_x = rbc_first_order_assembly(p);\n\njulia> r = klein_map(A, B; threshold = 1.0e-6);\n\njulia> size(r.g_x), size(r.h_x)\n((3, 2), (2, 2))\n\nr.g_x is the static policy: each row gives one jump variable (consumption, output, investment) as a linear function of the predetermined state (k, z). r.h_x is the state transition, and its spectrum sits strictly inside the unit disc by construction:\n\njulia> maximum(abs, eigvals(r.h_x)) < 1\ntrue\n\nTo differentiate any summary of the policy with respect to the model parameters, compose the assembly with klein_map and hand the closure to DifferentiationInterface.jl. The backend (ForwardDiff, Enzyme reverse, etc.) is just an argument:\n\njulia> function policy_summary(p)\n           A, B, _ = rbc_first_order_assembly(p)\n           r = klein_map(A, B; threshold = 1.0e-6)\n           return sum(r.g_x) + sum(r.h_x)\n       end;\n\njulia> ∇p = gradient(policy_summary, AutoForwardDiff(), p);\n\njulia> (∇p[5], ∇p[6])                              # σ and Ω_1 don't enter the pencil\n(0.0, 0.0)\n\nSwapping to Enzyme reverse mode is a one-line change:\n\nusing Enzyme\nusing DifferentiationInterface: AutoEnzyme\n∇p = gradient(policy_summary, AutoEnzyme(mode = Enzyme.Reverse), p)\n\nBoth backends return the same gradient to floating-point round-off; the test suite exercises this directly.\n\nThe other DSGE pencils used by the test/benchmark suites have analogous precomputed fixture bundles but no parametric assembly. Each one returns a (; A_schur, B_schur, B_shock, g_x, h_x, n_x) NamedTuple so the same destructuring works downstream: RBCExampleMatrices.dp_rbc_sv_first_order_inputs (RBC with stochastic volatility, n = 8), SGUExampleMatrices.dp_sgu_first_order_inputs (n = 15), FVGQExampleMatrices.dp_fvgq_first_order_inputs (n = 38), and SW07ExampleMatrices.dp_sw07pfeifer_first_order_inputs (n = 42).\n\nDifferentiate the policy with Enzyme reverse mode:\n\nusing Enzyme\n\nloss(A, B) = (r = klein_map(A, B); sum(r.g_x) + sum(r.h_x))\n\nA_bar = zero(A); B_bar = zero(B)\nEnzyme.autodiff(\n    Reverse, loss, Active,\n    Duplicated(copy(A), A_bar),\n    Duplicated(copy(B), B_bar),\n)\n\nA_bar and B_bar now contain bar A and bar B as defined in Klein Policy Map.","title":"Quick start: parameters → pencil → policy"},{"category":"section","location":"#Loaded-extensions","page":"Home","text":"The custom rules live in package extensions and load automatically when the corresponding AD package is in scope:\n\nExtension Trigger\nMatrixEquationsADForwardDiffExt using ForwardDiff\nMatrixEquationsADEnzymeExt using Enzyme\nMatrixEquationsADStaticArraysExt using StaticArrays\n\nThe StaticArrays extension supplies SMatrix dispatches for klein_map (requires explicit Val(n_x) for type-stable output sizing) and lyapdkr.","title":"Loaded extensions"}]
}
